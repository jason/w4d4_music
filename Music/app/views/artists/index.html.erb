<h1>Hit Artists of the Day . 5</h1>

<ul>
  <% @artists.each do |artist| %>
    <li>
      <%= link_to format_link_title(artist), artist_path(artist) %>
      <%= link_to "Edit", edit_artist_path(artist) %>
      <%= link_to "Destroy!", artist_path(artist), method: :delete %>
    </li>
  <% end %>
</ul>

<%= link_to "Add an Artist", new_artist_path %>